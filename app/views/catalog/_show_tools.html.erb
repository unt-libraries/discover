<%-
  # Compare with render_document_functions_partial helper, and
  # _document_functions partial. BL actually has two groups
  # of document-related tools. "document functions" by default
  # contains Bookmark functionality shown on both results and
  # item view. While "document tools" contains external export type
  # functions by default only on detail.

-%>
<%# TODO: Move items to separate categories and UI locations %>
<%# if show_doc_actions? %>
  <div class="card show-tools">
    <h3 class="card-header show-tools-heading"><%= t('blacklight.tools.title') %></h3>

    <div class="card-body show-tools-values list-group list-group-flush">
      <%= render_show_doc_actions @document do |config, inner| %>
        <div class="list-group-item show-tools-item <%= config.key %>" data-tool-item="<%= config.key %>">
          <%= inner %>
        </div>
      <% end %>

      <% unless @document[:id].nil? -%>
        <div class="list-group-item show-tools-item refworks-tool" data-tool-item="refworks">
          <%= link_to_request_refworks(@document) %>
        </div>

        <% unless Rails.application.credentials[Rails.env.to_sym][:COVID_RESTRICTED] -%>
          <div class="list-group-item show-tools-item illiad-tool" data-tool-item="illiad">
            <%= link_to_request_illiad(@document) %>
          </div>
        <% end -%>
      <% end -%>

      <% unless @document[:oclc_numbers].nil? -%>
        <div class="list-group-item show-tools-item citation-tool" data-tool-item="citation">
          <%= link_to_worldcat_citations(@document) %>
        </div>
      <% end -%>

      <div class="list-group-item show-tools-item related-articles-tool" data-tool-item="related-articles">
        <%= link_to_summon_search(@document) %>
      </div>

      <div class="list-group-item show-tools-item google-scholar-tool" data-tool-item="google-scholar">
        <%= link_to_google_scholar_search(@document) %>
      </div>

      <div class="list-group-item show-tools-item catalog-tool" data-tool-item="catalog">
        <%= link_to_old_catalog(@document) %>
      </div>
    </div>
  </div>
<%# end %>
