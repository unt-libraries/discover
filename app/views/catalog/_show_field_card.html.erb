<%# render general fields without :display property %>
<% fields ||= presenter.fields_to_render.select { |name, f| f[:display] == display_name.to_sym }%>

<div class="card item-<%= display_name %> breathing-room">
  <div class="card-body">
    <h2 class="card-title show-heading-title__more-details <%= display_name.parameterize %>-heading"><%= display_name.titlecase %></h2>
    <dl class="dl-invert document-metadata">
      <% fields.each do |field_name, field, field_presenter| -%>
        <%= render partial: 'show_field',
                   locals: {
                       display_name: display_name,
                       field_name: field_name,
                       field: field,
                       field_presenter: field_presenter
                   }
        %>
      <% end -%>
    </dl>
  </div>
</div>
